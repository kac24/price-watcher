
# A simple automated market monitor in Go

Developed a CLI-based monitoring tool in Go to track real-time asset valuations via REST APIs.  Implemented custom JSON marshaling with Go structs and managed automated alert dispatching via HTTP POST requests to mobile notification services.
